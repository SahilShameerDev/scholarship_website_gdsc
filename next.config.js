/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/scholarship_website_gdsc",
    output: "export",  // <=== enables static exports
  reactStrictMode: true,
}

module.exports = nextConfig
